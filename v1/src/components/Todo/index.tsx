import { Box, Flex, Spacer, Text } from '@chakra-ui/react';
import React from 'react';
import axios from 'axios';

const main = () => {
  return (
    <Flex>
      <Box>
        <Text>Hi this is the todo page component</Text>
      </Box>
    </Flex>
  );
};

export default main;
